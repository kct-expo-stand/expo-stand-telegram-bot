# Telegram Bot для сбора заявок на обучение

Telegram-бот на Python (aiogram 3.x) для сбора контактных данных и направлений обучения с сохранением в SQLite и Google Sheets.

## Возможности

- Сбор номера телефона через Telegram Contact или ручной ввод
- Выбор направления обучения через inline-кнопки (FrontEnd, BackEnd, Design, GameDev, Project Manager)
- Локальное сохранение в SQLite
- Автоматическая отправка данных в Google Sheets
- Защита от повторных заявок (опционально)
- Модульная архитектура с FSM

## Установка

### 1. Клонирование репозитория

```bash
git clone https://github.com/kct-expo-stand/expo-stand-telegram-bot.git
cd expo-stand-telegram-bot
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 3. Настройка Google Sheets

#### Шаг 1: Создайте Google Таблицу

1. Откройте [Google Sheets](https://sheets.google.com)
2. Создайте новую таблицу
3. Переименуйте первый лист в **"Лист1"** (или оставьте как есть)
4. (Опционально) Добавьте заголовки в первую строку:
   - A1: `Дата и время`
   - B1: `User ID`
   - C1: `Username`
   - D1: `Имя`
   - E1: `Телефон`
   - F1: `Направление`

#### Шаг 2: Откройте Apps Script

1. В меню таблицы выберите **Расширения** → **Apps Script**
2. Удалите весь код в редакторе
3. Вставьте следующий код:

```javascript
function doPost(e) {
  try {
    var sheet = SpreadsheetApp.getActiveSpreadsheet().getSheetByName("Лист1");
    if (!sheet) {
      sheet = SpreadsheetApp.getActiveSpreadsheet().getSheets()[0];
    }

    var raw = e.postData ? e.postData.contents : null;
    if (!raw) {
      return ContentService.createTextOutput("NO_DATA");
    }

    var data = JSON.parse(raw);

    sheet.appendRow([
      new Date(),
      data.user_id || "",
      data.username || "",
      data.first_name || "",
      data.phone || "",
      data.program || ""
    ]);

    return ContentService.createTextOutput("OK");
  } catch (err) {
    return ContentService.createTextOutput("ERROR: " + err);
  }
}

function doGet(e) {
  return ContentService.createTextOutput("OK");
}
```

4. Нажмите **💾 Сохранить проект** (или `Ctrl+S`)
5. Дайте проекту имя, например: `TelegramBotWebhook`

#### Шаг 3: Разверните веб-приложение

1. Нажмите **Развернуть** → **Новое развертывание**
2. Нажмите на шестеренку ⚙️ возле **"Выберите тип"**
3. Выберите **Веб-приложение**
4. Настройте параметры:
   - **Описание**: `Telegram Bot Webhook`
   - **Запуск от имени**: `От моего имени (ваш email)`
   - **Доступ к приложению**: `Все`
5. Нажмите **Развернуть**
6. Подтвердите разрешения (Google попросит авторизацию)
7. **Скопируйте URL веб-приложения** (выглядит как `https://script.google.com/macros/s/AKfycby.../exec`)

#### Шаг 4: Сохраните URL

Скопированный URL понадобится для файла `.env`

### 4. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
BOT_TOKEN=your_bot_token_here
SHEET_ENDPOINT=https://script.google.com/macros/s/AKfycby.../exec
ALLOW_DUPLICATE_SUBMISSIONS=false
```

**Параметры:**

- `BOT_TOKEN` — токен вашего бота от [@BotFather](https://t.me/BotFather)
- `SHEET_ENDPOINT` — URL веб-приложения из Google Apps Script
- `ALLOW_DUPLICATE_SUBMISSIONS` — разрешить повторные заявки от одного пользователя:
  - `false` — пользователь может отправить заявку только один раз
  - `true` — пользователь может отправлять заявки повторно

### 5. Подготовка изображения

Убедитесь, что в папке `images/` находится файл `violet-genius.png` (или другое изображение, которое будет отправляться пользователям)

## Запуск

```bash
python main.py
```

## Структура проекта

```
expo-stand-telegram-bot/
├── database/
│   └── db.py              # Работа с SQLite
├── handlers/
│   └── user_handlers.py   # Обработчики сообщений и callback
├── images/
│   └── violet-genius.png  # Изображение для отправки
├── keyboards/
│   └── builders.py        # Генераторы клавиатур
├── states/
│   └── states.py          # FSM состояния
├── utils/
│   └── sheets.py          # Интеграция с Google Sheets
├── .env                   # Переменные окружения (создать вручную)
├── .env.example           # Пример переменных окружения
├── .gitignore             # Исключения для Git
├── config.py              # Загрузка конфигурации
├── main.py                # Точка входа
├── requirements.txt       # Зависимости Python
└── README.md              # Документация
```

## База данных

Бот автоматически создаёт SQLite базу `leads.db` (чтобы в случае ошибок при работе с Google Sheets исключить потерю заявок) со следующей структурой:

| Поле       | Тип      | Описание                      |
|------------|----------|-------------------------------|
| id         | INTEGER  | Автоинкремент, первичный ключ |
| user_id    | INTEGER  | Telegram User ID              |
| username   | TEXT     | Telegram username             |
| first_name | TEXT     | Имя пользователя              |
| phone      | TEXT     | Номер телефона                |
| program    | TEXT     | Выбранное направление         |
| timestamp  | DATETIME | Дата и время создания записи  |

## Как это работает

1. Пользователь отправляет `/start`
2. Бот отправляет изображение и запрашивает номер телефона (через контакт или ручной ввод)
3. Бот спрашивает о направлении обучения (inline-кнопки)
4. Данные сохраняются в локальную SQLite базу
5. Данные отправляются POST-запросом в Google Sheets
6. Бот отправляет подтверждение с изображением

## Разработка

Проект использует:
- **aiogram 3.x** — асинхронный фреймворк для Telegram Bot API
- **aiosqlite** — асинхронная работа с SQLite
- **aiohttp** — HTTP-клиент для отправки в Google Sheets
- **python-dotenv** — загрузка переменных окружения

## Лицензия

MIT License
